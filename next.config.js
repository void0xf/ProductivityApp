/** @type {import('next').NextConfig} */
const nextConfig = {
  // Using default output directory for Vercel compatibility
  // distDir: "build",
  // Enable React strict mode for better development experience
  reactStrictMode: true,
};

module.exports = nextConfig;
